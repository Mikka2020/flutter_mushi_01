import 'package:flutter/material.dart';

class MushiList extends StatelessWidget {
  const MushiList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: (const Text('Mushi List')),
    );
  }
}
